# Catalyst Faucet Web (PRODUCTION) - example
# Copy to: /opt/catalyst-faucet/env/web.env
#
# NOTE: NEXT_PUBLIC_* values are public by design (they ship to the browser).
# In this project, they are typically baked at build time into the client bundle.

# Base URL of the faucet API (no trailing slash).
# Recommended when using this repo's Caddy routing:
# - API is reachable at https://<domain>/api
NEXT_PUBLIC_FAUCET_API_BASE_URL=https://faucet.example.com/api

# Cloudflare Turnstile site key (widget key).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=REPLACE_ME

# Network display name.
NEXT_PUBLIC_NETWORK_NAME=Catalyst Testnet

# Tx link template (placeholders supported: <txHash>, {txHash}, :txHash).
NEXT_PUBLIC_EXPLORER_TX_URL_TEMPLATE=https://explorer.catalystnet.org/tx/<txHash>

# Testnet-only guard (hex or decimal; comma-separated).
NEXT_PUBLIC_ALLOWED_CHAIN_IDS=0xbf8457c

