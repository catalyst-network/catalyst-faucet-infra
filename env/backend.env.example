# Catalyst Faucet Backend (PRODUCTION) - example
# Copy to: /opt/catalyst-faucet/env/backend.env and chmod 600
#
# Secrets: FAUCET_PRIVATE_KEY, TURNSTILE_SECRET_KEY, ADMIN_TOKEN, IP_HASH_SALT
# MUST NOT be committed to git.

# --- Chain / safety guard ---
CHAIN_ID=0xbf8457c
RPC_URL=https://your-catalyst-testnet-rpc.example
GENESIS_HASH=0xREPLACE_ME_32_BYTE_HASH

# Faucet hot wallet key (testnet only).
FAUCET_PRIVATE_KEY=0xREPLACE_ME

# Amount in integer units, e.g. 1000
FAUCET_AMOUNT=1000

# Abuse controls
COOLDOWN_HOURS=24
GLOBAL_RPM=60
ENABLE_COUNTRY_LIMIT=false
ENABLE_ASN_LIMIT=false

# Turnstile server-side secret (captcha verification).
TURNSTILE_SECRET_KEY=REPLACE_ME

# Admin endpoints shared secret. Consider also restricting by IP in Caddy.
ADMIN_TOKEN=REPLACE_ME

# Long random string (>=16 chars). Used to hash client IPs before storing.
IP_HASH_SALT=REPLACE_WITH_LONG_RANDOM_STRING

# Internal service URLs (recommended to set explicitly in production).
REDIS_URL=redis://redis:6379
DATABASE_URL=postgresql://postgres:REPLACE_ME_STRONG_PASSWORD@postgres:5432/faucet?schema=public

# Optional logging
# LOG_LEVEL=info

