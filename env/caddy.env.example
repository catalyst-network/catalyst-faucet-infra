# Environment used by docker-compose.prod.yml (Caddy + stack)
# Copy to: /opt/catalyst-faucet/env/caddy.env

# Domain that will serve the faucet UI.
FAUCET_DOMAIN=faucet.example.com

# Optional: override built image names (if you publish to a registry).
# FAUCET_BACKEND_IMAGE=ghcr.io/your-org/catalyst-faucet-backend:sha-...
# FAUCET_WEB_IMAGE=ghcr.io/your-org/catalyst-faucet-web:sha-...

